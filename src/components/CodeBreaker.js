import React from "react";

export default function CodeBreaker() {
  console.log("✅ CodeBreaker.js Loaded!"); // ✅ This confirms the file is loading

  return (
    <div style={{ background: "red", height: "100vh", color: "white", fontSize: "30px", textAlign: "center", paddingTop: "100px" }}>
      <h1>✅ CodeBreaker is Working</h1>
    </div>
  );
}
